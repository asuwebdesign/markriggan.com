{% extends "_layout" %}
{% set title = "Work" %}
{% set bodyClass = 'theme-dark' %}

{% block content %}
<article>
  <div class="band" data-topic="expertise" data-hero="true" data-align="center">
    <div class="wrapper wrapper--condensed">
      <h1>{{ entry.featuredTitle }}</h1>
      <p>{{ entry.featuredSubtitle }}</p>
    </div>
  </div>

  <div class="wrapper">
    <section>
      <div class="projects" data-size="3">
      {% for entry in craft.entries.section('projects').find() %}
        <div class="project">
          <a href="{{ entry.url }}">
            <h2>{{ entry.title }}</h2>
            <img src="https://cdn.dribbble.com/users/63407/screenshots/3659995/dribbble_prison_cellular.png" />
          </a>
        </div>
      {% endfor %}
      </div>
    </section>
  </div>

  <section>
    <div class="wrapper">
      <h2>Work in Progress</h2>
      <ul class="shots"></ul>
    </div>
  </section>
</article>
{% endblock %}

{% block scripts %}
<script src="{{ siteUrl }}js/dribbble.js"></script>
<script>
$.jribbble.setToken('21e6d63a0005cdec7cd62f3a450845523b7c59151bad82d32544f470037961be');
$.jribbble.users('markr').shots().then(function(res) {
  var html = [];
  res.forEach(function(shot) {
    html.push('<li>');
    html.push('<a href="' + shot.html_url + '" target="_blank">');
    html.push('<img src="' + shot.images.normal + '" draggable="false">');
    html.push('</a></li>');
  });
  $('.shots').html(html.join(''));
}).then(function() {
  var api = $('#barba-wrapper').data('jsp');
  api.reinitialise();
});
</script>
{% endblock %}
